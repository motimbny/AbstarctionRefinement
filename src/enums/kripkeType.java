package enums;
/**
 * There are 3 types of kripke structure.
 * Regular = The original Kripke given
 * Under= The kripke after using under approximations
 * Over = The kripke after using over approximations
 * @author Matthew
 *
 */
public enum kripkeType 
{
	Regular,
	Under,
	Over
}
